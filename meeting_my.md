**ASR-бэкенд**: faster-whisper (он же то, что часто называют Whisper-Fast)
**Надстройка**: WhisperX (для батчей, тайм-кодов и спикеров)
**Модель**: large-v3 (если влезает в GPU), иначе medium.en

## А теперь по терминам и сравнению.

### 1. Что такое Whisper

Whisper — это базовая модель распознавания речи от OpenAI:

Архитектура: encoder–decoder Transformer (похож на GPT, но по аудио). 
Поддержка >100 языков, умеет и распознавать, и переводить (например, из любого языка в английский). 

Сильные стороны: устойчивость к шуму, акцентам и «живой» речи; очень низкий WER по сравнению с большинством опенсорс-альтернатив. 
voicewriter.io
Ограничения «ванильного» Whisper:

Даёт примерные сегментные таймстемпы (на фразу), а не по словам — погрешность в секунды. 
Нормального батчинга для длинных файлов нет «из коробки». 
Нет встроенной диаризации (кто когда говорит).

### 2. Что такое WhisperX

WhisperX — это фреймворк поверх Whisper, а не отдельная модель. Он берёт те же веса Whisper (large-v2, large-v3 и т.д.), но оборачивает их в расширенный пайплайн: 

⚡ Batched inference:
Может прогонять длинные аудио батчами, что даёт до ~70× real-time на large-v2/v3 при хорошем GPU. 

🪶 faster-whisper backend:
Вместо оригинальной реализации использует faster-whisper (CTranslate2), что сильно экономит память и ускоряет инференс; large-v2/v3 можно крутить < 8 ГБ VRAM с beam search. 

🎯 Word-level timestamps:
После черновой расшифровки WhisperX запускает forced alignment на основе wav2vec2 (или других фонетических моделей), подгоняя каждое слово под спектрограмму. Это даёт точные таймкоды на уровне слова и снижает WER на длинных записях. 

👯‍♂️ Speaker diarization:
Интеграция с pyannote.audio — получаем пометку, какой спикер сказал конкретный фрагмент. 

🗣️ VAD preprocessing:
Использует voice activity detection, чтобы обрезать куски без речи, уменьшить галлюцинации и лучше паковать батчи. 

Важно: качество распознавания (текст) у WhisperX ≈ у того же Whisper-large-v2/v3, потому что веса одни и те же. Разница — в точности таймингов и удобстве работы с длинными файлами. 


## 3. Whisper-Fast vs Faster-Whisper

Чаще всего под «Whisper-Fast» имеют в виду либо:

библиотеку faster-whisper, либо

какой-то CLI/обёртку вокруг неё.

**faster-whisper** — это низкоуровневая реализация Whisper на CTranslate2: 
* до 4× быстрее, чем оригинальный openai/whisper,
* меньше потребление памяти,
* есть квантование (int8 и др.) для CPU/GPU,
* поддерживает все стандартные модели (tiny → large-v3). 


По сути:

Whisper (оригинал) — эталонная реализация.

faster-whisper — оптимизированный движок для тех же моделей.

WhisperX — пайплайн, который часто использует faster-whisper внутри для скорости + добавляет тайминги и диаризацию. 

### 4. Сравнение Whisper / WhisperX / Faster-Whisper для DubPipeline

Для вашего сценария (лекции EN → RU, локальный оффлайн-луп):

Качество (WER)

#### Whisper vs faster-whisper:

Качество почти идентично — это те же веса модели, только другой инференс-движок. 

#### WhisperX:

Текст такой же или чуть лучше на длинных записях за счёт более аккуратных сегментов и forced alignment; главное — идеальные таймкоды до слова, что важно для дубляжа/субтитров. 

Скорость

Обычный Whisper (Python) — условно 1×.

faster-whisper — до 4× быстрее при той же модели. 

WhisperX:
Сам по себе чуть тяжелее из-за выравнивания и диаризации, но за счёт батчинга и faster-whisper на длинных лекциях итоговая скорость отличная: вплоть до десятков × real-time, если использовать GPU и large-v2/v3. 

## Фичи

* Задача	Whisper	faster-whisper	WhisperX
* Просто текст	✅	✅ (быстрее)	✅ (через тот же Whisper)
* Таймкоды по фразам	✅ (но грубо)	✅	✅
* Таймкоды по словам	❌	❌	✅ (forced alignment)
* Диаризация (кто говорит)	❌	❌	✅ (pyannote)
* Большие батчи / длинные	Ограничено	Лучше	🚀 (как раз для этого и сделан)

### 5. Другие варианты, о которых стоит знать

Если кратко (по свежему обзору вариантов Whisper): 
Modal~~~~

### **insanely-fast-whisper**

Максимальный throughput на топовых GPU (A100 и т.п.), умеет очень агрессивные оптимизации (flash-attn и др.). 

Больше похож на «готовый CLI-инструмент», чем на гибкий конструктор пайплайнов.

### whisper.cpp

Оптимизирован под CPU (ggml/gguf), хорошо для ноутбуков/мало-VRAM железа.

Удобен, если нужно запускать вообще без нормальной GPU.

Отдельные диаризаторы: pyannote.audio, NVIDIA NeMo и т.п. — если захочется собрать свой пайплайн (Whisper + VAD + diarization) руками. 
Vast AI
Для нашего проекта, где важно качество + удобные тайминги для дубляжа, а не только голая скорость, WhisperX + faster-whisper выглядит оптимальным компромиссом. 

## 6. Предложение по выбору для [DubPipeline]

С учётом ваших требований (лекции, EN→RU, локальный оффлайн, нормальная видеодлина):

Бэкенд распознавания: faster-whisper

Даёт хороший баланс скорость/память/качество.

Легко завернуть в свой сервис (Python или отдельный микросервис).

### Надстройка: WhisperX

Заберёт на себя:
точные таймкоды по словам, нарезку по спикерам, VAD, батчинг, работу с длинным аудио.
Прекрасно ложится на задачу дубляжа/субтитров.

Модель:

Если GPU тянет: large-v3 (лучшее качество, особенно для английских лекций). 

Если VRAM ограничен (<=8 ГБ) — начать с medium.en или large-v2 в float16 с небольшим beam size.

### Whisper-Fast как отдельный выбор:

Если под этим вы имеете просто обёртку вокруг faster-whisper, то разницы по качеству с нашим стеком не будет — только по UX/CLI.

Для проекта-«конструктора» (как DubPipeline) логичнее опираться на чистый faster-whisper + WhisperX, а не на чью-то готовую утилиту.

<details> <summary>Немного теории (совсем кратко, можно пропустить)</summary>

### ASR-модели (Whisper)

Whisper — это Transformer, который смотрит на спектрограмму аудио и генерирует текст токен за токеном, как GPT генерирует текст из текста. Модель обучена на сотнях тысяч часов аудио, поэтому хорошо обобщает на акценты, шум и т.д. 

### Forced alignment (WhisperX + wav2vec2)
Whisper даёт примерно: «эта реплика началась в 10.2 c и закончилась в 13.7 c». WhisperX берёт готовый текст и аудио, прогоняет через фонетический выравниватель (wav2vec2 / CTC-модель) и аккуратно выставляет границы каждого слова по максимуму совпадения со спектрограммой. 


### Діаризация (pyannote)
Diarization = «кто когда говорит». Пайплайн обычно:
VAD → нарезка на речевые сегменты → извлечение эмбеддингов голоса → кластеризация (спикер A/B/C). pyannote даёт state-of-the-art качество и готовые пайплайны. 


</details>