[DubPipeline] Шаг 1 — Термины Whisper / WhisperX / Faster-Whisper
Обзор

Этот документ описывает ключевые термины и технологии, которые мы будем использовать в проекте DubPipeline для распознавания речи, сегментации, диаризации и подготовки данных к дубляжу.

Покрываем:

Whisper

WhisperX

faster-whisper (часто обозначаемый как Whisper-Fast)

альтернативы

рекомендации по выбору стека

краткая теория (внизу документа)

1. Whisper — базовая модель ASR

Whisper — open-source модель распознавания речи от OpenAI.

Ключевые характеристики

Transformer encoder → decoder.

Поддержка >100 языков.

Высокая устойчивость к шуму, акцентам, живой речи.

Может одновременно транскрибировать и переводить.

Ограничения оригинальной реализации

Таймкоды неточные (уровень фразы, погрешность до секунд).

Нет диаризации (кто говорит).

Нет удобного батчинга на длинные файлы.

Высокая нагрузка и потребление VRAM.

2. WhisperX — расширенный пайплайн вокруг Whisper

WhisperX — не отдельная модель, а надстройка над Whisper + faster-whisper.

Что добавляет WhisperX

⚡ Batched inference — работает в десятки раз быстрее на длинных аудио.

🪶 faster-whisper backend — экономит VRAM и ускоряет инференс.

🎯 Word-level timestamps — точные таймкоды по словам через forced alignment.

👯 Speaker diarization — интеграция с pyannote.audio.

🗣️ VAD — фильтрация тишины и ускорение.

Важно

WhisperX не меняет текстовое качество Whisper — только улучшает тайминги, сегментацию и обработку длинных файлов.

3. faster-whisper (Whisper-Fast)

faster-whisper — это оптимизированный движок инференса моделей Whisper на базе CTranslate2.

Плюсы

До 4× быстрее оригинального Whisper.

Низкое потребление VRAM.

CPU/GPU квантование (int8 … fp16).

Поддержка всех моделей (tiny → large-v3).

Отношение к WhisperX

WhisperX обычно использует faster-whisper как backend.

4. Сравнение технологий
Качество текста (WER)

Whisper = faster-whisper — одинаковые веса → одинаковое качество.

WhisperX — чуть лучше на больших файлах из-за более аккуратных сегментов и forced alignment.

Скорость
Технология	Скорость
Whisper (оригинал)	~1× real-time
faster-whisper	до ~4× быстрее
WhisperX	зависит от alignment, но при длинных файлах может давать десятки × real-time благодаря батчингу
Функциональность
Возможность	Whisper	faster-whisper	WhisperX
Распознавание текста	✔	✔ (быстрее)	✔
Таймкоды фраз	✔ (грубо)	✔	✔
Таймкоды слов	✘	✘	✔
Диаризация	✘	✘	✔
VAD	частично	✔	✔
Работа с длинными файлами	слабая	хорошая	отличная
5. Альтернативы
insanely-fast-whisper

Максимальный throughput на top-GPU.

Агрессивные оптимизации (flash-attention).

Полезна, если нужно обрабатывать тонны аудио.

whisper.cpp (ggml/gguf)

Для CPU/минимальных систем.

Отличен на ноутбуках или слабых ПК.

Отдельные диаризаторы

pyannote.audio (state-of-the-art)

NVIDIA NeMo

Можно вручную собрать custom-пайплайн.

6. Рекомендованный стек для DubPipeline

С учётом требований: локально, EN → RU, лекции, длинные файлы, дубляж, точные таймкоды.

Идеальная конфигурация

ASR backend: faster-whisper

Пайплайн: WhisperX

Модель:

при наличии VRAM: large-v3

при VRAM ≤ 8 ГБ: medium.en или large-v2 (beam=1–3)

Почему такой стек

Максимальное качество.

Быстрое выполнение на длинных лекциях.

Идеально подходит для дубляжа (точные слова + VAD + спикеры).

7. Немного теории
<details> <summary>Развернуть краткую теорию</summary>
Как работает Whisper

Whisper — это Transformer, который принимает спектрограмму аудио и генерирует текст токен за токеном (как GPT генерирует текст).

Forced alignment

WhisperX делает forced alignment с помощью моделей типа wav2vec2/CTC, подгоняя каждое слово под аудио.
Результат: таймкоды точностью до 10–30 мс.

Диаризация

Состоит из этапов:

VAD

Разбиение на речевые сегменты

Извлечение voice-embeddings

Кластеризация (спикер A/B/C)

WhisperX использует pyannote.audio, который считается одним из лучших open-source решений.